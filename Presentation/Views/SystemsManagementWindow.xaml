<Window x:Class="Nivtropy.Presentation.Views.SystemsManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление ходами"
        Width="700"
        Height="500"
        WindowStartupLocation="CenterOwner"
        Background="#FFF5F5F5">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Основное содержимое -->
        <Grid Grid.Row="0" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Левая панель: Список систем -->
            <Border Grid.Column="0"
                    BorderBrush="#FFBCC7D3"
                    BorderThickness="1"
                    CornerRadius="2"
                    Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="#40000000" BlurRadius="4" ShadowDepth="1" Direction="270" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <Border Grid.Row="0"
                            Background="#FFF8F9FA"
                            BorderBrush="#FFE0E0E0"
                            BorderThickness="0,0,0,1"
                            Padding="10,8">
                        <TextBlock Text="Системы ходов"
                                   FontWeight="SemiBold"
                                   FontSize="12"
                                   Foreground="#2B579A" />
                    </Border>

                    <!-- Список систем -->
                    <ListBox Grid.Row="1"
                             x:Name="SystemsListBox"
                             SelectionChanged="SystemsListBox_SelectionChanged"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding DisplayName}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding RunCount, StringFormat=({0})}"
                                               FontSize="10"
                                               Foreground="#707070"
                                               Margin="4,0,0,0"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Кнопки управления системами -->
                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="8">
                        <Button Content="➕ Создать"
                                Click="CreateSystem_Click"
                                Padding="10,4"
                                Margin="0,0,4,0"
                                FontSize="10"
                                Background="White"
                                BorderBrush="#FFACACAC"
                                BorderThickness="1"
                                Cursor="Hand" />
                        <Button Content="✏ Переименовать"
                                Click="RenameSystem_Click"
                                Padding="10,4"
                                Margin="0,0,4,0"
                                FontSize="10"
                                Background="White"
                                BorderBrush="#FFACACAC"
                                BorderThickness="1"
                                Cursor="Hand" />
                        <Button Content="🗑 Удалить"
                                Click="DeleteSystem_Click"
                                Padding="10,4"
                                FontSize="10"
                                Background="White"
                                BorderBrush="#FFACACAC"
                                BorderThickness="1"
                                Cursor="Hand" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Правая панель: Ходы выбранной системы -->
            <Border Grid.Column="2"
                    BorderBrush="#FFBCC7D3"
                    BorderThickness="1"
                    CornerRadius="2"
                    Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="#40000000" BlurRadius="4" ShadowDepth="1" Direction="270" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <Border Grid.Row="0"
                            Background="#FFF8F9FA"
                            BorderBrush="#FFE0E0E0"
                            BorderThickness="0,0,0,1"
                            Padding="10,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       x:Name="RunsHeaderText"
                                       Text="Ходы системы"
                                       FontWeight="SemiBold"
                                       FontSize="12"
                                       Foreground="#2B579A"
                                       VerticalAlignment="Center" />
                            <ComboBox Grid.Column="1"
                                      x:Name="MoveToSystemComboBox"
                                      DisplayMemberPath="Name"
                                      FontSize="10"
                                      MinWidth="120"
                                      Height="24"
                                      Background="White"
                                      BorderBrush="#FFACACAC"
                                      ToolTip="Переместить выбранные ходы в другую систему" />
                        </Grid>
                    </Border>

                    <!-- Список ходов -->
                    <ListBox Grid.Row="1"
                             x:Name="RunsListBox"
                             SelectionMode="Multiple"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0"
                                              IsChecked="{Binding IsActive}"
                                              Margin="0,0,6,0"
                                              VerticalAlignment="Center"
                                              ToolTip="Активен" />

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Header}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding CombinedStats}"
                                               FontSize="9"
                                               Foreground="#707070"
                                               Margin="4,0,0,0"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Нижняя панель: Кнопки -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#FFE0E0E0"
                BorderThickness="0,1,0,0"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="⬆ Переместить в выбранную систему"
                        Click="MoveRuns_Click"
                        Padding="10,6"
                        FontSize="11"
                        HorizontalAlignment="Left"
                        Background="White"
                        BorderBrush="#FFACACAC"
                        BorderThickness="1"
                        Cursor="Hand" />

                <Button Grid.Column="1"
                        Content="Закрыть"
                        Click="Close_Click"
                        Padding="20,6"
                        FontSize="11"
                        Background="White"
                        BorderBrush="#FFACACAC"
                        BorderThickness="1"
                        Cursor="Hand" />
            </Grid>
        </Border>
    </Grid>
</Window>
