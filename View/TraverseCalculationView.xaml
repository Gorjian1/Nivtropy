<UserControl x:Class="Nivtropy.Views.TraverseCalculationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#FFF2F2F2"
                BorderBrush="#FFE0E0E0"
                BorderThickness="1"
                Padding="10"
                CornerRadius="4">
            <StackPanel>
                <TextBlock Text="{Binding RunCaption}"
                           FontWeight="SemiBold"
                           FontSize="14" />
                <TextBlock Text="{Binding TotalsText}"
                           Margin="0,4,0,0" />
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1"
                  Margin="0,8,0,8"
                  ItemsSource="{Binding ClassSummaries}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  RowHeaderWidth="0"
                  SelectionUnit="FullRow"
                  Height="120">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Класс" Binding="{Binding Code}" Width="100" />
                <DataGridTextColumn Header="Количество" Binding="{Binding Count}" Width="110" />
                <DataGridTextColumn Header="ΣΔh" Binding="{Binding SumDisplay}" Width="120" />
                <DataGridTextColumn Header="Средняя" Binding="{Binding AverageDisplay}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Measurements}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  SelectionUnit="FullRow"
                  AlternationCount="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="50" />
                <DataGridTextColumn Header="Seq" Binding="{Binding Sequence}" Width="60" />
                <DataGridTextColumn Header="Порядок" Binding="{Binding ShotIndex}" Width="80" />
                <DataGridTextColumn Header="Точка" Binding="{Binding Point}" Width="160" />
                <DataGridTextColumn Header="Mode" Binding="{Binding Mode}" Width="90" />
                <DataGridTextColumn Header="Класс" Binding="{Binding NormalizedClass}" Width="80" />
                <DataGridTextColumn Header="Rb" Binding="{Binding BackSight, StringFormat=F4}" Width="90" />
                <DataGridTextColumn Header="Rf" Binding="{Binding ForeSight, StringFormat=F4}" Width="90" />
                <DataGridTextColumn Header="Δh" Binding="{Binding DeltaH, StringFormat=+0.0000;-0.0000;0.0000}" Width="110" />
                <DataGridTextColumn Header="ΣΔh" Binding="{Binding CumulativeDelta, StringFormat=+0.0000;-0.0000;0.0000}" Width="110" />
                <DataGridTextColumn Header="L, м" Binding="{Binding Distance, StringFormat=0.00}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   Margin="0,8,0,0"
                   Text="{Binding Analysis}"
                   TextWrapping="Wrap"
                   Foreground="#FF333333" />
    </Grid>
</UserControl>
