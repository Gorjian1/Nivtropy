<UserControl x:Class="Nivtropy.Views.TraverseCalculationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Nivtropy.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <converters:MethodDeltaConverter x:Key="MethodDeltaConverter" />
        <converters:GroupClosureConverter x:Key="GroupClosureConverter" />
        <CollectionViewSource x:Key="RowsGrouped" Source="{Binding Rows}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="LineName" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Margin="8,8,8,4"
                    Orientation="Horizontal">
            <TextBlock Text="Расчёт нивелирного хода"
                       FontSize="14"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" Margin="24,0,0,0">
                <TextBlock Text="Метод:" VerticalAlignment="Center" />
                <TextBlock Text="{Binding SelectedMethod.Display}"
                           FontWeight="SemiBold"
                           Margin="6,0,0,0"
                           VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0">
                <TextBlock Text="Класс:" VerticalAlignment="Center" />
                <TextBlock Text="{Binding SelectedClass.Display}"
                           FontWeight="SemiBold"
                           Margin="6,0,0,0"
                           VerticalAlignment="Center" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="8,4,8,4" Orientation="Horizontal">
            <TextBlock Text="Станций:" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding StationsCount}" Margin="4,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="•" Margin="12,0,12,0" VerticalAlignment="Center" Foreground="#FF606060" />
            <TextBlock Text="ΣΔh:" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Closure, StringFormat=+0.0000;-0.0000;0.0000}" Margin="4,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="•" Margin="12,0,12,0" VerticalAlignment="Center" Foreground="#FF606060" />
            <TextBlock Text="Допуск:" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding AllowableClosure, StringFormat=0.0000}" Margin="4,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="м" Margin="2,0,0,0" VerticalAlignment="Center" Foreground="#FF606060" />
            <TextBlock Text="•" Margin="12,0,12,0" VerticalAlignment="Center" Foreground="#FF606060" />
            <TextBlock Text="ΣHDback:" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding TotalBackDistance, StringFormat=0.00}" Margin="4,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="м" Margin="2,0,0,0" VerticalAlignment="Center" Foreground="#FF606060" />
            <TextBlock Text="•" Margin="12,0,12,0" VerticalAlignment="Center" Foreground="#FF606060" />
            <TextBlock Text="ΣHDfore:" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding TotalForeDistance, StringFormat=0.00}" Margin="4,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="м" Margin="2,0,0,0" VerticalAlignment="Center" Foreground="#FF606060" />
        </StackPanel>

        <!-- Панель управления известными высотами -->
        <Border Grid.Row="2" Margin="8,4,8,4" BorderBrush="#FF6B9BD1" BorderThickness="1" Background="#FFF0F4FF">
            <StackPanel Margin="8,4">
                <TextBlock Text="Установить известную отметку (высоту Z) точке:" FontWeight="SemiBold" Margin="0,0,0,4" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Точка:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox x:Name="PointComboBox" Width="150" VerticalAlignment="Center" Margin="0,0,8,0" SelectionChanged="PointComboBox_SelectionChanged">
                        <ComboBoxItem Tag="Back" Content="{Binding SelectedRow.BackCode, StringFormat='Задняя: {0}'}" />
                        <ComboBoxItem Tag="Fore" Content="{Binding SelectedRow.ForeCode, StringFormat='Передняя: {0}'}" />
                    </ComboBox>
                    <TextBlock Text="Высота Z, м:" VerticalAlignment="Center" Margin="8,0,8,0" />
                    <TextBox x:Name="HeightTextBox" Width="120" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Button Content="Установить" Padding="8,4" Click="SetHeightButton_Click" IsEnabled="{Binding CanSetHeight}" />
                    <Button Content="Очистить" Padding="8,4" Margin="8,0,0,0" Click="ClearHeightButton_Click" IsEnabled="{Binding CanClearHeight}" />
                    <TextBlock Text="(Выберите строку в таблице)" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="Gray" FontStyle="Italic" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Margin="8" BorderBrush="#FFB0B0B0" BorderThickness="1">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Padding="4"
                           Background="#FFEAEAEA"
                           Text="{Binding ClosureVerdict}" />
                <DataGrid x:Name="RowsGrid"
                          ItemsSource="{Binding Source={StaticResource RowsGrouped}}"
                          SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          Margin="0"
                          RowHeaderWidth="0"
                          EnableRowVirtualization="True"
                          SelectionUnit="FullRow">
                    <DataGrid.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.ContainerStyle>
                                <Style TargetType="GroupItem">
                                    <Setter Property="Margin" Value="0,4,0,0" />
                                </Style>
                            </GroupStyle.ContainerStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border Background="#E3F2FD" BorderBrush="#90CAF9" BorderThickness="1" Padding="8" CornerRadius="4">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding ItemCount, StringFormat=' • Станций: {0}'}" Margin="12,0,0,0" Foreground="#FF606060" />
                                            <TextBlock Text=" • ΣΔh: " Margin="12,0,0,0" Foreground="#FF606060" />
                                            <TextBlock Text="{Binding Converter={StaticResource GroupClosureConverter}, StringFormat=+0.0000;-0.0000;0.0000}"
                                                       FontWeight="SemiBold"
                                                       Foreground="#FF1976D2" />
                                            <TextBlock Text=" м" Foreground="#FF606060" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                            <GroupStyle.Panel>
                                <ItemsPanelTemplate>
                                    <DataGridRowsPresenter />
                                </ItemsPanelTemplate>
                            </GroupStyle.Panel>
                        </GroupStyle>
                    </DataGrid.GroupStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding Index}" Width="60">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Номер станции в пределах хода" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Станция" Binding="{Binding Station}" Width="240">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Переход: задняя точка → передняя точка" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Rb, м" Binding="{Binding Rb_m, StringFormat=F4}" Width="100">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Отсчёт по задней рейке (Reading Back)" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Rf, м" Binding="{Binding Rf_m, StringFormat=F4}" Width="100">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Отсчёт по передней рейке (Reading Forward)" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Δh, м" Binding="{Binding DeltaH, StringFormat=+0.0000;-0.0000;0.0000}" Width="120">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Превышение: Rb - Rf (положительное значение = передняя точка выше задней)" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Δh (метод), м" Width="140">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Превышение с учётом направления хода (BF: прямое, FB: обратное)" />
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <TextBlock.Text>
                                            <MultiBinding Converter="{StaticResource MethodDeltaConverter}" StringFormat="+0.0000;-0.0000;0.0000">
                                                <Binding Path="DeltaH" />
                                                <Binding Path="DataContext.MethodOrientationSign" RelativeSource="{RelativeSource AncestorType={x:Type DataGrid}}" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="HDback, м" Binding="{Binding HdBack_m, StringFormat=0.00}" Width="110">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Горизонтальное расстояние до задней рейки" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="HDfore, м" Binding="{Binding HdFore_m, StringFormat=0.00}" Width="110">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Горизонтальное расстояние до передней рейки" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="H зад., м" Binding="{Binding BackHeightDisplay}" Width="150">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Высота задней точки (известная или рассчитанная)" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="H перед., м" Binding="{Binding ForeHeightDisplay}" Width="150">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="ToolTip" Value="Высота передней точки (известная или рассчитанная)" />
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
