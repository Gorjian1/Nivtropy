<UserControl x:Class="Nivtropy.Views.TraverseCalculationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                <TextBlock Text="Класс:" FontWeight="SemiBold" VerticalAlignment="Center" />
                <ComboBox Margin="8,0,0,0"
                          Width="220"
                          ItemsSource="{Binding Classes}"
                          SelectedItem="{Binding SelectedClass}"
                          DisplayMemberPath="Title" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Начальная отметка, м:" VerticalAlignment="Center" />
                <TextBox Margin="8,0,0,0"
                         Width="110"
                         Text="{Binding StartHeight, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </DockPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Rows}"
                  SelectedItem="{Binding SelectedRow}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  Margin="8"
                  RowHeaderWidth="0"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HdWithinTolerance}" Value="False">
                            <Setter Property="Background" Value="#FFFFE8E8" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HdWithinTolerance}" Value="True">
                            <Setter Property="Background" Value="#FFE8FFF0" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Ход" Binding="{Binding LineName}" Width="150" IsReadOnly="True" />
                <DataGridTextColumn Header="№" Binding="{Binding Index}" Width="60" />
                <DataGridTextColumn Header="Задняя рейка" Binding="{Binding BackCode}" Width="140" />
                <DataGridTextColumn Header="Передняя рейка" Binding="{Binding ForeCode}" Width="140" />
                <DataGridTextColumn Header="RB, м" Width="90">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Rb_m" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" StringFormat="F4" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="RF, м" Width="90">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Rf_m" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" StringFormat="F4" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Δh, м" Binding="{Binding DeltaH, StringFormat=F4}" Width="90" IsReadOnly="True" />
                <DataGridTextColumn Header="HD задн., м" Width="110">
                    <DataGridTextColumn.Binding>
                        <Binding Path="HdBack_m" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" StringFormat="F2" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="HD перед., м" Width="110">
                    <DataGridTextColumn.Binding>
                        <Binding Path="HdFore_m" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" StringFormat="F2" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="|BF-FB|, м" Binding="{Binding HdImbalance_m, StringFormat=F2}" Width="110" IsReadOnly="True" />
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusNote}" Width="180" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Margin="8" Padding="12" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="Высоты" FontWeight="SemiBold" />
                    <TextBlock>
                        <TextBlock.Text>
                            <Binding Path="SumDeltaH" StringFormat="ΣΔh = {0:+0.0000;-0.0000;0.0000} м" TargetNullValue="ΣΔh = —" />
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock>
                        <TextBlock.Text>
                            <Binding Path="AverageDelta" StringFormat="Δh¯ = {0:+0.0000;-0.0000;0.0000} м" TargetNullValue="Δh¯ = —" />
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock>
                        <TextBlock.Text>
                            <Binding Path="FinishHeight" StringFormat="Финиш: {0:0.000} м" TargetNullValue="Финиш: —" />
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="Визиры" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding TotalBackLength, StringFormat=ΣBF = {0:0.00} м}" />
                    <TextBlock Text="{Binding TotalForeLength, StringFormat=ΣFB = {0:0.00} м}" />
                    <TextBlock Text="{Binding TotalImbalance, StringFormat=Δ(BF-FB) = {0:0.00} м}" />
                </StackPanel>
                <StackPanel Grid.Column="2" Spacing="4">
                    <TextBlock Text="Вердикты" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding HdVerdict}" TextWrapping="Wrap" />
                    <TextBlock>
                        <TextBlock.Text>
                            <Binding Path="AllowedMisclosure" StringFormat="Допуск невязки: ±{0:0.000} м" TargetNullValue="Допуск невязки: —" />
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock Text="{Binding MisclosureVerdict}" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
