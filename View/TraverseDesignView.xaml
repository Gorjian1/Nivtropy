<UserControl x:Class="Nivtropy.Views.TraverseDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="3"
                   Text="Проектирование нивелирного хода"
                   FontWeight="SemiBold"
                   FontSize="16"
                   Margin="0,0,0,12" />

        <GroupBox Grid.Row="1" Header="Исходные данные" Padding="12">
            <StackPanel Spacing="8">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <TextBlock Text="Класс:" VerticalAlignment="Center" />
                    <ComboBox Width="220"
                              ItemsSource="{Binding Classes}"
                              SelectedItem="{Binding SelectedClass}"
                              DisplayMemberPath="Title" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Начальная отметка, м" />
                    <TextBox Text="{Binding DesignStartHeight, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Целевая отметка, м" />
                    <TextBox Text="{Binding DesignTargetHeight, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Количество станций" />
                    <TextBox Text="{Binding StationsCount, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <Button Content="Рассчитать"
                        Command="{Binding ComputeCommand}"
                        HorizontalAlignment="Left"
                        Padding="16,4" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Column="2" Grid.Row="1" Header="Результаты" Padding="12">
            <StackPanel Spacing="8">
                <TextBlock Text="Средняя Δh на станцию:" />
                <TextBlock>
                    <TextBlock.Text>
                        <Binding Path="AverageDeltaPerStation" StringFormat="{}{0:+0.000;-0.000;+0.000} м" TargetNullValue="—" />
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Text="Рекомендуемая длина визирования:" />
                <TextBlock>
                    <TextBlock.Text>
                        <Binding Path="RecommendedSightLength" StringFormat="{}{0:0} м" TargetNullValue="—" />
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Text="Допустимая разность BF/FB:" />
                <TextBlock>
                    <TextBlock.Text>
                        <Binding Path="RecommendedDifference" StringFormat="{}{0:0.00} м" TargetNullValue="—" />
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Text="Прогнозный допуск невязки:" />
                <TextBlock>
                    <TextBlock.Text>
                        <Binding Path="ProjectedMisclosureAllowance" StringFormat="±{0:0.000} м" TargetNullValue="—" />
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Text="Рекомендации:" FontWeight="SemiBold" />
                <Border Background="#FFF8F8F8" BorderBrush="#FFE0E0E0" BorderThickness="1" Padding="8">
                    <TextBlock Text="{Binding Recommendation}" TextWrapping="Wrap" />
                </Border>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
