<UserControl x:Class="Nivtropy.Views.TraverseDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1200">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*" />
            <ColumnDefinition Width="3*" MinWidth="300" />
        </Grid.ColumnDefinitions>

        <!-- Левая панель: Таблица с данными -->
        <GroupBox Grid.Column="0" Header="Сгенерированные данные измерений" Margin="0,0,8,0">
            <Grid>
                <!-- Таблица -->
                <DataGrid
                          ItemsSource="{Binding Measurements}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          GridLinesVisibility="All"
                          VerticalGridLinesBrush="#FFE0E0E0"
                          HorizontalGridLinesBrush="#FFE0E0E0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding Index}" Width="50" />
                        <DataGridTextColumn Header="Точка" Binding="{Binding PointCode}" Width="*" MinWidth="80" />
                        <DataGridTextColumn Header="Station" Binding="{Binding StationCode}" Width="*" MinWidth="100" />
                        <DataGridTextColumn Header="Rb, м" Binding="{Binding Rb_m, StringFormat=F4}" Width="100" />
                        <DataGridTextColumn Header="Rf, м" Binding="{Binding Rf_m, StringFormat=F4}" Width="100" />
                        <DataGridTextColumn Header="HD назад, м" Binding="{Binding HD_Back_m, StringFormat=F2}" Width="110" />
                        <DataGridTextColumn Header="HD вперёд, м" Binding="{Binding HD_Fore_m, StringFormat=F2}" Width="110" />
                        <DataGridTextColumn Header="Высота, м" Binding="{Binding Height_m, StringFormat=F4}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Правая панель: Настройки и управление -->
        <ScrollViewer Grid.Column="1" Margin="8,0,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                    <!-- Кнопки управления -->
                    <GroupBox Header="Управление" Margin="0,0,0,16">
                        <StackPanel Margin="8">
                            <Button Content="Сгенерировать"
                                    Command="{Binding GenerateCommand}"
                                    Height="32"
                                    Margin="0,0,0,8" />
                            <Button Content="Экспортировать"
                                    Command="{Binding ExportCommand}"
                                    Height="32" />
                        </StackPanel>
                    </GroupBox>

                    <!-- Формат экспорта -->
                    <GroupBox Header="Формат экспорта" Margin="0,0,0,16">
                        <StackPanel Margin="8">
                            <RadioButton Content="Нивелир (*.txt)"
                                        IsChecked="{Binding FormatNivelir}"
                                        GroupName="ExportFormat" />
                        </StackPanel>
                    </GroupBox>

                    <!-- Настройки генерации -->
                    <GroupBox Header="Настройки генерации" Margin="0,0,0,16">
                        <StackPanel Margin="8">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="СКО для измерений:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding StdDevMeasurement, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Стандартное отклонение для обычных измерений"
                                        Width="60" />
                                <TextBlock Text="мм" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="СКО для грубых ошибок:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding StdDevGrossError, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Стандартное отклонение для грубых ошибок"
                                        Width="60" />
                                <TextBlock Text="мм" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Частота грубых ошибок:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding GrossErrorFrequency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Каждая N-ная станция будет иметь грубую ошибку (0 = отключить)"
                                        Width="60" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Мин. расстояние до рейки:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding MinDistance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Минимальное расстояние до рейки"
                                        Width="60" />
                                <TextBlock Text="м" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Макс. расстояние до рейки:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding MaxDistance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Максимальное расстояние до рейки"
                                        Width="60" />
                                <TextBlock Text="м" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Высота отсчета на рейке:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding InstrumentHeightBase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Базовая высота отсчета Rb на рейке (центр диапазона)"
                                        Width="60" />
                                <TextBlock Text="м" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Вариация отсчета:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding InstrumentHeightVariation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Случайная вариация отсчета Rb (±)"
                                        Width="60" />
                                <TextBlock Text="м" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Разброс длины станции:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding StationLengthSpread, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Максимальная разница между HD назад и HD вперед на одной станции"
                                        Width="60" />
                                <TextBlock Text="м" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Смещение превышения:" VerticalAlignment="Center" Width="150" />
                                <TextBox Text="{Binding ElevationOffset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTip="Случайное отклонение превышений от CSV (0 = строго из CSV, >0 = варьируются с сохранением невязки)"
                                        Width="60" />
                                <TextBlock Text="мм" Margin="4,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- Источник данных -->
                    <GroupBox Header="Источник данных" Margin="0,0,0,16">
                        <StackPanel Margin="8">
                            <TextBlock Text="Файл с результатами расчётов:" Margin="0,0,0,4" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding SourceFilePath, TargetNullValue=Не выбран}"
                                      TextWrapping="Wrap"
                                      Foreground="#FF606060"
                                      Margin="0,0,0,8" />
                            <Button Content="Открыть файл"
                                   Command="{Binding OpenFileCommand}"
                                   HorizontalAlignment="Stretch"
                                   Height="30" />
                        </StackPanel>
                    </GroupBox>

                    <!-- Профиль хода -->
                    <GroupBox Header="Профиль хода" Margin="0,0,0,0">
                        <Grid Margin="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" MinHeight="250" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Заголовок с кнопками выбора хода -->
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0" Content="◀" Width="30" Height="30"
                                        Command="{Binding PreviousTraverseCommand}"
                                        ToolTip="Предыдущий ход" />

                                <TextBlock Grid.Column="1"
                                          Text="{Binding CurrentTraverseName}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          FontWeight="Bold"
                                          FontSize="14" />

                                <Button Grid.Column="2" Content="▶" Width="30" Height="30"
                                        Command="{Binding NextTraverseCommand}"
                                        ToolTip="Следующий ход" />
                            </Grid>

                            <!-- Canvas для рисования профиля -->
                            <Border Grid.Row="1"
                                   Background="White"
                                   BorderBrush="#FFE0E0E0"
                                   BorderThickness="1">
                                <Canvas x:Name="ProfileCanvas" />
                            </Border>

                            <!-- Макс/Мин значения -->
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0"
                                       HorizontalAlignment="Center">
                                <TextBlock Text="Макс: " FontWeight="Bold" />
                                <TextBlock Text="{Binding MaxHeight, StringFormat=F3}" Margin="0,0,15,0" />
                                <TextBlock Text="Мин: " FontWeight="Bold" />
                                <TextBlock Text="{Binding MinHeight, StringFormat=F3}" />
                                <TextBlock Text=" м" Margin="2,0,0,0" />
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
    </Grid>
</UserControl>
