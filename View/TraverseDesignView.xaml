<UserControl x:Class="Nivtropy.Views.TraverseDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="8">
            <TextBlock Text="Проектирование исходных данных"
                       FontSize="16"
                       FontWeight="SemiBold" />
            <TextBlock Margin="0,4,0,0"
                       Text="Распределение невязки и расчёт проектных высот." />
        </StackPanel>

        <Grid Grid.Row="1" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Первая строка -->
            <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock Text="Общая длина хода, м" FontWeight="SemiBold" />
                <TextBlock Text="{Binding TotalDistance, StringFormat=0.000}" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1">
                <TextBlock Text="Фактическая ΣΔh" FontWeight="SemiBold" />
                <TextBlock Text="{Binding ActualClosure, StringFormat=+0.0000;-0.0000;0.0000}" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="2">
                <TextBlock Text="Допустимая невязка" FontWeight="SemiBold" />
                <TextBlock Text="{Binding AllowableClosure, StringFormat=±0.0000}" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="3">
                <TextBlock Text="Статус проверки" FontWeight="SemiBold" />
                <TextBlock Text="{Binding ClosureStatus}" FontWeight="Bold" />
            </StackPanel>

            <!-- Вторая строка -->
            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,16,0,0">
                <TextBlock Text="Начальная отметка" FontWeight="SemiBold" />
                <TextBlock Text="{Binding StartHeight, StringFormat=0.000}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,16,0,0">
                <TextBlock Text="Целевая ΣΔh" FontWeight="SemiBold" />
                <TextBlock Text="{Binding TargetClosure, StringFormat=+0.0000;-0.0000;0.0000}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,16,0,0">
                <TextBlock Text="Проектная ΣΔh" FontWeight="SemiBold" />
                <TextBlock Text="{Binding DesignedClosure, StringFormat=+0.0000;-0.0000;0.0000}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,16,0,0">
                <TextBlock Text="Средняя коррекция" FontWeight="SemiBold" />
                <TextBlock Text="{Binding CorrectionPerStation, StringFormat=+0.0000;-0.0000;0.0000}" />
            </StackPanel>
        </Grid>

        <!-- Панель управления известными высотами -->
        <Border Grid.Row="2" Margin="8,4,8,4" BorderBrush="#FF6B9BD1" BorderThickness="1" Background="#FFF0F4FF">
            <StackPanel Orientation="Horizontal" Margin="8,4">
                <TextBlock Text="Установить высоту точке:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                <TextBlock Text="{Binding SelectedRow.PointCode}" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0" MinWidth="50" />
                <TextBox x:Name="HeightTextBox" Width="100" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Button Content="Установить" Padding="8,4" Click="SetHeightButton_Click" IsEnabled="{Binding CanSetHeight}" />
                <Button Content="Очистить" Padding="8,4" Margin="8,0,0,0" Click="ClearHeightButton_Click" IsEnabled="{Binding CanClearHeight}" />
                <TextBlock Text="(Выберите строку в таблице для установки высоты точке)" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="Gray" FontStyle="Italic" />
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Margin="8" BorderBrush="#FFB0B0B0" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Rows}"
                      SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Margin="0"
                      RowHeaderWidth="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Binding="{Binding Index}" Width="50" />
                    <DataGridTextColumn Header="Станция" Binding="{Binding Station}" Width="150" />
                    <DataGridTextColumn Header="Длина, м" Binding="{Binding Distance_m, StringFormat=0.000}" Width="100" />
                    <DataGridTextColumn Header="Δh исх., м" Binding="{Binding OriginalDeltaH, StringFormat=+0.0000;-0.0000;0.0000}" Width="120" />
                    <DataGridTextColumn Header="Попр., м" Binding="{Binding Correction, StringFormat=+0.0000;-0.0000;0.0000}" Width="110" />
                    <DataGridTextColumn Header="Δh уравн., м" Binding="{Binding AdjustedDeltaH, StringFormat=+0.0000;-0.0000;0.0000}" Width="130" />
                    <DataGridTextColumn Header="Проектн. отметка, м" Binding="{Binding HeightDisplay}" Width="180" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
